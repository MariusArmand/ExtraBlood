{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ExtraBlood",
            "type": "cppdbg",
            "request": "launch",
            "linux": {
                "program": "${workspaceRoot}/extrablood",
            },
            "windows": {
                "program": "${workspaceRoot}/extrablood.exe",
            },
            "cwd": "${workspaceRoot}",
        }
    ]
}